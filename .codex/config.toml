[agents]
enabled = true
max_concurrent_threads_per_session = 2
max_depth = 1
# Untyped bounded work stays inexpensive. Select the Builder profile explicitly
# for coordinated work that passes the balanced opportunity gate.
default_subagent_model = "gpt-5.6-luna"
default_subagent_reasoning_effort = "low"
