@AGENTS.md

## Claude Code adapter

Claude Code uses `.claude/skills` as its native Skill location. The installer links every canonical BoundedFreedom Skill there. If a matching Skill is absent from the catalog, read its `SKILL.md` under `.agents/skills/` directly. Keep Claude-only syntax and model aliases out of portable Skills.
